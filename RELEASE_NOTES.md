# Release Notes

## Unreleased

目前没有尚未发布的变更。

## v1.3.1 — 2026-07-25

本版更新 CLEC 逐字稿校对词库，未改动主要工作流程。

### 词库更新

- 更新词库。

### 兼容性与发布边界

- 主 `SKILL.md`、页面模板与逐字稿整理器没有变更。
- JavaScript 旧版兼容工具继续保留。
- 缺省仍为 `pending-editorial-review` 与 `publish_to_website: false`。

## v1.3.0 — 2026-07-25

本版刷新技能主流程，保留完整的 CLEC 来源、逐字稿与 Obsidian 规则，同时减少重复说明，让 Codex 更容易依序运行。

### 添加

- 正式整理版逐字稿开头统一使用「阅读版仍需人工校对、讲者标示方式、专有名词校正」三个备注。
- 基本词库添加 `support@clec168.com`，并维持 `Claude Tag` 为用户确认的标准词。
- 将必要参考数据整理为按需读取入口，避免每次加载不相关细节。

### 变更

- `SKILL.md` 改为九步运行流程：范围、来源、转录、主页、标题、整理版、匿名化、批量校对、验证。
- CC、局部 ASR、完整 ASR 的品质细节集中到 `references/transcript-quality-gates.md`。
- 专有名词集中到 `references/clec-transcription-lexicon.md`，00574 细节保留在已确认范例。
- 不再把 `media-transcript --engine auto` 写成固定接口，改由 `media-transcript` 自动探测来源。
- Python 整理器作为正式工具；JavaScript 整理器保留旧设置兼容性。
- README 同步更新为 `v1.3.0`。

### 验证与发布边界

- 保留原始来源、原始字幕／ASR、简体原稿、整理版与人工修订的分层。
- 缺省仍为 `pending-editorial-review` 与 `publish_to_website: false`。
- GitHub 发布包排除 `scripts/__pycache__/`。

## v1.2.0 — 2026-07-25

本版补齐逐字稿品质门槛、读者页与后端证据分层，并加入以用户确认词库批量校对多集整理稿的安全流程。

### 添加

- 添加逐字稿品质门槛，改以「产出可人工审阅初稿的总时间」比较 CC 与 ASR 工作流。
- 明确定义 CC、局部 ASR、完整 ASR 的选择条件与多区段抽查范围。
- 添加节目尾端与最后 30 秒验证；无音频依据的 ASR 续写不得进入衍生稿。
- 添加指定逐字稿文件夹的批量校对流程，只修改阅读版／整理版 Markdown。
- 批量修改前后以 SHA-256 验证 TXT、SRT、VTT 等源文件完全一致。
- 添加 UTF-8 乱码停止条件及交付前残留误辨词扫描。
- 词库添加 CLEC 常见平台、人名、ETF、房地产、医疗与 AI 词汇，包括 Odysee、Clubhouse、LINE 群组、QQQ、QQQM、Charles Schwab、Lisa、Anthropic 与 Claude 等。

### 变更

- 补充标点、自然分段、有限去赘词及不可改变原意的编辑边界。
- 统一来源分层、匿名化、身分词阻挡与人工审核门槛。
- 节目主页的逐字稿区改为只嵌入整理版；原始稿链接、模型、segment 数量与转录流程留在后端。
- 移除读者主页中的转录状态、整理进度、待办、来源状态表与证据限制。
- 保留内容辨识例外：同号不同内容或短篇后缀可能误并时，主页仍显示「编号重复」警示。
- 短代号改采完整词界与长词优先级，避免 `QQ` 改坏 `QQQM`，或 `OD` 改坏其他英文单字。
- 需要依句意处理的词不再只做机械替换，例如 `amburance` 依上下文整理为 `ambulance` 或「进急诊」。

### 验证

- 00571、00572、00573、00574 的 5 份 Markdown 整理稿已套用已确认词库。
- 所有已确认误辨词与暂存乱码扫描为零。
- TXT、SRT、VTT 等非 Markdown 源文件前后 SHA-256 完全一致。
- 缺省仍为 `publish_to_website: false`，未自动发布网站或数据库。

## v1.1.0 — 2026-07-25

本版把 `clec-episode-organizer` 从单集整理流程扩充为 CLEC 内容整理的集成入口，并统一 Obsidian 单集主页格式。

### 添加

- 支持用户指定日期区间，先创建期间清单，再依五位数编号合并同集来源。
- 纳入 X、pCloud、Google Drive、Bilibili、Odysee、RSS／Podcast 的来源角色与确认状态。
- 统一由 `media-transcript` 负责字幕取得、媒体下载及语音辨识。
- 添加 CC 优先流程：少数疑义只用同集音频局部补正，字幕整体不可用时才重跑全长 ASR。
- 添加 CLEC 词汇校对数据库的查找与可信度规则。
- 添加长片主持人候选与核对规则，但不把候选当成已确认身分。
- 添加 Python 阅读版逐字稿产生器。

### 变更

- 固定三层阅读标题为：
  - `30 秒内看懂`
  - `3–5 分钟理解主要论证`
  - `愿意深读的人：证据、限制与延伸数据`
- 固定深读区包含范围说明、名词说明、来源对照、证据限制、逐字稿与整理进度。
- 整理版逐字稿固定嵌入单集主页；原始简体稿保留为链接。
- 强化主页文件名、YAML `title` 与 H1 必须完全一致的规则。
- 更新 00574 已确认范例，使其符合新版标题及章节层级。
- README 补充技能分工、日期区间范例与新版页面骨架。

### 兼容性与发布状态

- 保留既有 `canonical_id`、逐字稿分层及 Obsidian Wiki link 规则。
- 缺省仍为 `pending-editorial-review`。
- 缺省仍为 `publish_to_website: false`。
- 本版没有自动发布网站、CMS 或数据库。

### 验证

- `clec-episode-organizer` skill 结构验证通过。
- 00571、00572、00573、00574 四页已用相同固定骨架检查通过。

## v1.0.0

首版创建单集来源集成、三层阅读、逐字稿分段、学员匿名与 Obsidian 同页嵌入流程。

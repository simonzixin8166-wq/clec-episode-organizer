# Release Notes

## Unreleased

目前沒有尚未發布的變更。

## v1.3.0 — 2026-07-25

本版重新整理技能主流程，保留完整的 CLEC 來源、逐字稿與 Obsidian 規則，同時減少重複說明，讓 Codex 更容易依序執行。

### 新增

- 正式整理版逐字稿開頭統一使用「閱讀版仍需人工校對、講者標示方式、專有名詞校正」三個備註。
- 基本詞庫新增 `support@clec168.com`，並維持 `Claude Tag` 為使用者確認的標準詞。
- 將必要參考資料整理為按需讀取入口，避免每次載入不相關細節。

### 變更

- `SKILL.md` 改為九步執行流程：範圍、來源、轉錄、主頁、標題、整理版、匿名化、批次校對、驗證。
- CC、局部 ASR、完整 ASR 的品質細節集中到 `references/transcript-quality-gates.md`。
- 專有名詞集中到 `references/clec-transcription-lexicon.md`，00574 細節保留在已確認範例。
- 不再把 `media-transcript --engine auto` 寫成固定介面，改由 `media-transcript` 自動探測來源。
- Python 整理器作為正式工具；JavaScript 整理器保留舊設定相容性。
- README 同步更新為 `v1.3.0`。

### 驗證與發布邊界

- 保留原始來源、原始字幕／ASR、繁體原稿、整理版與人工修訂的分層。
- 預設仍為 `pending-editorial-review` 與 `publish_to_website: false`。
- GitHub 發布包排除 `scripts/__pycache__/`。

## v1.2.0 — 2026-07-25

本版補齊逐字稿品質門檻、讀者頁與後端證據分層，並加入以使用者確認詞庫批次校對多集整理稿的安全流程。

### 新增

- 新增逐字稿品質門檻，改以「產出可人工審閱初稿的總時間」比較 CC 與 ASR 工作流。
- 明確定義 CC、局部 ASR、完整 ASR 的選擇條件與多區段抽查範圍。
- 新增節目尾端與最後 30 秒驗證；無音訊依據的 ASR 續寫不得進入衍生稿。
- 新增指定逐字稿資料夾的批次校對流程，只修改閱讀版／整理版 Markdown。
- 批次修改前後以 SHA-256 驗證 TXT、SRT、VTT 等原始檔完全一致。
- 新增 UTF-8 亂碼停止條件及交付前殘留誤辨詞掃描。
- 詞庫新增 CLEC 常見平台、人名、ETF、房地產、醫療與 AI 詞彙，包括 Odysee、Clubhouse、LINE 群組、QQQ、QQQM、Charles Schwab、Lisa、Anthropic 與 Claude 等。

### 變更

- 補充標點、自然分段、有限去贅詞及不可改變原意的編輯邊界。
- 統一來源分層、匿名化、身分詞阻擋與人工審核門檻。
- 節目主頁的逐字稿區改為只嵌入整理版；原始稿連結、模型、segment 數量與轉錄流程留在後端。
- 移除讀者主頁中的轉錄狀態、整理進度、待辦、來源狀態表與證據限制。
- 保留內容辨識例外：同號不同內容或短篇後綴可能誤併時，主頁仍顯示「編號重複」警示。
- 短代號改採完整詞界與長詞優先順序，避免 `QQ` 改壞 `QQQM`，或 `OD` 改壞其他英文單字。
- 需要依句意處理的詞不再只做機械替換，例如 `amburance` 依上下文整理為 `ambulance` 或「進急診」。

### 驗證

- 00571、00572、00573、00574 的 5 份 Markdown 整理稿已套用已確認詞庫。
- 所有已確認誤辨詞與暫存亂碼掃描為零。
- TXT、SRT、VTT 等非 Markdown 原始檔前後 SHA-256 完全一致。
- 預設仍為 `publish_to_website: false`，未自動發布網站或資料庫。

## v1.1.0 — 2026-07-25

本版把 `clec-episode-organizer` 從單集整理流程擴充為 CLEC 內容整理的整合入口，並統一 Obsidian 單集主頁格式。

### 新增

- 支援使用者指定日期區間，先建立期間清單，再依五位數編號合併同集來源。
- 納入 X、pCloud、Google Drive、Bilibili、Odysee、RSS／Podcast 的來源角色與確認狀態。
- 統一由 `media-transcript` 負責字幕取得、媒體下載及語音辨識。
- 新增 CC 優先流程：少數疑義只用同集音訊局部補正，字幕整體不可用時才重跑全長 ASR。
- 新增 CLEC 詞彙校對資料庫的查找與可信度規則。
- 新增長片主持人候選與核對規則，但不把候選當成已確認身分。
- 新增 Python 閱讀版逐字稿產生器。

### 變更

- 固定三層閱讀標題為：
  - `30 秒內看懂`
  - `3–5 分鐘理解主要論證`
  - `願意深讀的人：證據、限制與延伸資料`
- 固定深讀區包含範圍說明、名詞說明、來源對照、證據限制、逐字稿與整理進度。
- 整理版逐字稿固定嵌入單集主頁；原始繁體稿保留為連結。
- 強化主頁檔名、YAML `title` 與 H1 必須完全一致的規則。
- 更新 00574 已確認範例，使其符合新版標題及章節層級。
- README 補充技能分工、日期區間範例與新版頁面骨架。

### 相容性與發布狀態

- 保留既有 `canonical_id`、逐字稿分層及 Obsidian Wiki link 規則。
- 預設仍為 `pending-editorial-review`。
- 預設仍為 `publish_to_website: false`。
- 本版沒有自動發布網站、CMS 或資料庫。

### 驗證

- `clec-episode-organizer` skill 結構驗證通過。
- 00571、00572、00573、00574 四頁已用相同固定骨架檢查通過。

## v1.0.0

首版建立單集來源整合、三層閱讀、逐字稿分段、學員匿名與 Obsidian 同頁嵌入流程。
